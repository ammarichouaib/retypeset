# retypeset parser layer
pydantic>=2.0
lxml>=4.9
pillow>=10.0

# Bundles a pandoc binary so no separate install / PATH edit is needed.
# If you already have pandoc on PATH this is optional.
pypandoc_binary>=1.13

# UI
streamlit>=1.33

# Renderers
python-docx>=1.1
cairosvg>=2.7          # SVG to PDF for the LaTeX route

# Optional: local training of the heading / section-role models
scikit-learn>=1.3
joblib>=1.3

# Tests
pytest>=7.0
